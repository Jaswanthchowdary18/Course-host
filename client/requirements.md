## Packages
framer-motion | Page transitions and detailed animations

## Notes
The application uses session-based authentication with credentials included in fetch requests.
API endpoints follow strict Zod schemas defined in shared/routes.
Promo code logic is handled partially on client (validation UI) and server (subscription).
Images will be sourced from Unsplash for course placeholders.
